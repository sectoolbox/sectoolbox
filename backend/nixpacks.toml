[phases.setup]
nixPkgs = ["nodejs_20", "wireshark-cli"]
nixOverlays = ["https://github.com/railwayapp/nixpkgs/archive/main.tar.gz"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
